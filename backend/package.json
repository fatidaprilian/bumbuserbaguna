{
  "name": "bumbuserbaguna-backend",
  "version": "0.2.0",
  "private": true,
  "description": "Backend modular monolith — Bumbuserbaguna academic productivity platform",
  "type": "module",
  "scripts": {
    "dev": "node --env-file=../.env --watch --import=tsx/esm src/main.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/main.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.20.0",
    "pino-pretty": "^13.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@fastify/helmet": "^13.0.1",
    "@fastify/jwt": "^9.1.0",
    "@fastify/rate-limit": "^10.2.2",
    "argon2": "^0.43.0",
    "dotenv": "^16.0.0",
    "fastify": "^5.3.2",
    "pg": "^8.20.0",
    "pino": "^9.0.0",
    "zod": "^4.3.6"
  }
}
